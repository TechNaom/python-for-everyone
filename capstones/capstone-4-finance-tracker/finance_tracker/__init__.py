"""finance_tracker -- a small OOP personal finance tracker.

Public surface: the Transaction/Income/Expense class hierarchy (models.py),
SQLite persistence (db.py), polymorphic reporting (reports.py), and the
argparse-based CLI (cli.py).
"""

__version__ = "1.0.0"
