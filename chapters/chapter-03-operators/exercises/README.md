# Chapter 3 Exercises: Operators

These exercises use what Chapter 3 covered: arithmetic, relational,
logical, and bitwise operators.

## How to run

```bash
python3 starter.py
```

## Task 1 — All seven arithmetic operators

Find `# TODO 1`. Given `a = 17` and `b = 5`, print the result of `+`, `-`,
`*`, `/`, `//`, `%`, and `**`.

## Task 2 — Range check

Find `# TODO 2`. Given `age = 25`, check whether it's between 18 and 65
(inclusive) using a single expression with `and`.

## Task 3 — Bitwise practice

Find `# TODO 3`. Given `x = 12` and `y = 10`, print `x & y`, `x | y`, and
`x ^ y`.

## Task 4 — Debug the Code

Find `# TODO 4`. This code is supposed to print the exact average of 7 and
2 (which is 3.5), but it always prints 3. Find and fix the bug.

## Checking your work

Compare your output against `solution.py`.
