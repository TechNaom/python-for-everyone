# expense_tracker

A tiny expense-tracking library -- the sample project this chapter's CI
workflow lints and tests. See `../README.md` (the chapter project README)
for the full walkthrough of how the workflow works and how to run everything
locally.

```bash
pip install -r requirements-dev.txt
pytest -v
ruff check .
```
