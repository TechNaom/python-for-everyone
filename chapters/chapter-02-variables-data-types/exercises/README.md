# Chapter 2 Exercises: Variables & Data Types

These exercises use what Chapter 2 covered: naming rules, `type()`/`id()`,
immutability, and type conversion.

## How to run

```bash
python3 starter.py
```

## Task 1 — Inspect three variables

Find `# TODO 1`. Create `favorite_number` (int), `pi_estimate` (float), and
`is_raining` (bool). Print each with its type using `type()`.

## Task 2 — Identity check

Find `# TODO 2`. Set `a = 5` and `b = 5`, print whether `id(a) == id(b)`,
and explain why in a comment or print statement.

## Task 3 — Real conversion

Find `# TODO 3`. Ask for the user's birth year with `input()`, convert it
to an `int`, and print their approximate age (assume the current year is
2026).

## Task 4 — Debug the Code

Find `# TODO 4`. This code raises a `TypeError` — find the bug and fix it
using a type conversion function from this chapter.

## Checking your work

Compare your output against `solution.py`. Your wording doesn't need to
match — the goal is a program that runs without errors and does what each
TODO asks.
